[project]
name = "vibestick"
version = "0.2.0"
description = "Windows VibeStick Bridge service with Reasonix integration."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
vibestick-bridge = "vibe_stick.__main__:main"
